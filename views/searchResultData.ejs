<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel ="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href='../assests/css/style.css'> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Result Management System</title>
</head>
<body>
    <header id="header">
        <nav>
            <div class="nav-container">
                <div class="text-center">
                    <b> Result Management System</b>
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <div class="row">
            <div class="col-lg-6 mx-auto mt-4">
                <div>
                    <a href="/student" class="btn btn-primary btn-small">Back To Search</a>
                </div>
                <br>
                <div class="card shadow ">
                    <div class="card-header bg-dark text-center">
                        <h4 class="text-light">Result</h4>
                    </div>
                    <div class="card-body p-4"><% if (result !== null) { %>
                        <form action="" style="text-align: center;">
                            <div class="mb-3" style="margin-right: 18px;">
                                <label for="rollNo"><b>Roll No.</b></label>
                                <input type="number" name="rollNo" value="<%= result.rollNo %>" id="" class="form-control form-control-lg" readonly required>
                            </div>
                            <div class="mb-3" style="margin-right: 2px;">
                                <label for="name"><b>Name</b></label>
                                <input type="text" name="name" value="<%= result.name %>" id="" class="form-control form-control-lg" readonly required>
                            </div>
                            <div class="mb-3">
                                <label for="score"><b>Score</b></label>
                                <input type="number" name="score" value="<%= result.score %>" id="" class="form-control form-control-lg" readonly required>
                            </div>
                        </form>                        
                        <% } else { %>
                            <p class="text-center" style="text-align: center;"> <b>No Record Found </b></p>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>   
</body>